{
  "$schema": "https://encore.dev/schemas/infra.schema.json",
  "sql_servers": [
    {
      "host": "postgres:5432",
      "databases": {
        "monitor": {
          "username": "postgres",
          "password": { "$env": "DB_PASSWORD" }
        },
        "site": {
          "username": "postgres",
          "password": { "$env": "DB_PASSWORD" }
        }
      }
    }
  ]
}
